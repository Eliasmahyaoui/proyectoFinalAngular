import { OrdenPrecioPipe } from './orden-precio-pipe';

describe('OrdenPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
